import 'package:flutter/material.dart';

var buttonStyle = ElevatedButton.styleFrom(primary: Colors.white10, onPrimary: Colors.black, padding: EdgeInsets.all(0), elevation: 0);
var currentTextStyle = TextStyle(color: Colors.redAccent);
var buttonTextMargin = EdgeInsets.symmetric(horizontal: 10);
var defaultTextStyle = TextStyle(color: Colors.black, letterSpacing: 1.3, fontSize: 15);
